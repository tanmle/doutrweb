/**
 * AdminTableStyles component
 * 
 * This component is no longer needed as all styles have been moved to AdminComponents.module.css
 * Kept for backward compatibility - can be removed if not used elsewhere
 */

export function AdminTableStyles() {
  return null;
}
